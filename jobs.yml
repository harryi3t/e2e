jobs:
  - name: e2e-job
    type: manifest
    steps:
      - GET input-image
      - PUT output-image
      - PUT output2-image
  
  - name: e2e-job2
    type: manifest
    steps:
      - GET input-image
      - GET input2-image
      - GET output-image
      - PUT output2-image
  
  - name: e2e-job3
    type: manifest
    steps:
      - GET input-image
      - GET input2-image
      - GET output-image
      - PUT output2-image
 
